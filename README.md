# LogoRRRs flatpak build 

This repository contains the flatpak yml definition for [LogoRRR](https://www.logorrr.app/).

To release a specific version replace `branch` declaration with the specific `revision` to build in `app.logorrr.LogoRRR.yml`.
